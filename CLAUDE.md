# PSEMS Backend — Context for Claude Code

## What this is
Backend for PSEMS (Project Scoring, Evaluation & Management System) — a CO3554 university project (5 credits) that must launch as a **real single-department faculty pilot by end of September 2026**. Full spec is in `docs/` (read `PSEMS_Comprehensive_Specification_v2.docx` for full detail — roles, 12-step CPI lifecycle, DB schema, ML endpoints; `PSEMS_Delivery_Roadmap.md` for the week-by-week build plan).

## Repo layout reality
This is one of **four separate repos** (polyrepo, not monorepo):
- `psems-backend` (this repo) — Node.js 20 / Express / TypeScript / Prisma / PostgreSQL+pgvector / Redis
- `psems-frontend` — React 18 / TypeScript / Vite / Tailwind
- `psems-ml-service` — Python 3.12 / FastAPI / scikit-learn / sentence-transformers
- `psems-infra` — docker-compose.yml for local Postgres+pgvector and Redis (shared by backend and ml-service)

Architecture is a **modular monolith**, not microservices: this backend is ONE deployable service with internal modules (auth, students, courses, groups, ideas, selection, allocation, evaluations, scoring, headjudge, scheduling, marks, analytics, notifications, files). Only the ML layer is a separate service, deliberately decoupled so the platform works even if ML is down.

Before touching this repo, bring up shared infra from the sibling `psems-infra` repo: `docker compose up -d` (Postgres+pgvector on 5432, Redis on 6379). `.env.example` in this repo has the connection strings.

## Timeline reality — read before making architectural decisions
Starting from zero on 2026-07-05, solo developer, ~12.5 weeks to a real pilot launch (not just a demo). The original spec's Gantt chart assumed 8 months — we've compressed to single-department pilot scope (matches the spec's own "Phase 1: single department" future roadmap) rather than cutting features. Non-negotiables regardless of time pressure: evaluator score isolation before Head Judge review, forced first-login password change + bcrypt + short-lived JWTs, RBAC enforced at both middleware AND service layer, audit logging on all writes, anonymized historical data before ML use. See the roadmap doc for the exact cut-list priority if something has to give.

## How the developer wants to work
The person building this explicitly wants to **learn the stack hands-on** (Prisma, pgvector, RBAC/JWT patterns, this is mostly new to them) — not just receive finished code. Default to explaining reasoning and letting them drive implementation, rather than silently generating large chunks of code. Check in on what actually got built at the end of each week against the roadmap table rather than assuming the plan survived contact with reality.

## Current phase
Week 6 COMPLETE as of 2026-07-13 (Week 1 = `07159cd`, Week 2 = `b732a26`, Week 3 = `d13ee4c`, Weeks 4 & 5 committed; Week 6 not yet committed). Shipped this week (three features, three new modules): **(1) Allocation** (`allocation` module, Step 7 / phase PROJECT_REGISTRATION): `generate` seeds `project_allocations` from each group's ACCEPTED selection; coordinator `override` (PUT per group, reassign idea/supervisor, source→COORDINATOR_OVERRIDE); `finalize` sets `CourseInstance.allocationsFinalizedAt` and locks (further changes → 409); GET returns map + unmatched groups. **(2) Evaluation config** (`evaluations` module, Step 8 / phase EVALUATION_CONFIG): `PUT /config` replace-all of stages+nested rubric criteria, validates stage weights sum to 100 and each stage's criteria weights sum to 100, blocked once submissions exist; `POST /stages/:id/evaluators` assigns pooled evaluators per stage; GET readable by any CPI participant. **(3) Proposal upload** (`files` module, Step 9): `POST /stages/:id/submission` (multipart, multer, 20MB). **Soft deadline** (ties to the earlier hard-vs-soft discussion): NOT hard phase-gated — service accepts once PROPOSAL_SUBMISSION window has opened and sets `isLate = now > window end`; before the window → 403. **Storage abstraction** (`files/storage.ts`): `StorageBackend` interface with a local-disk backend (tested default, writes to `STORAGE_DIR`/`uploads`, gitignored) and a Supabase backend selected by `STORAGE_BACKEND=supabase` env (SDK installed + lazy-imported; **written but unverified locally — needs real Supabase creds to test, same status as SMTP**). New tables: `project_allocations`, `evaluation_stages`, `rubric_criteria`, `stage_evaluators`, `submissions`. Added shared `getCpiEvaluatorId`. 28 tests green (7 suites), typecheck+lint clean. Next up: Week 7 — Scheduling (availability submission, timetable finalize); Evaluation execution (scoring interface, **evaluator isolation enforced**); Head Judge review (side-by-side, approve/request correction). **Evaluator score isolation before Head Judge review is a non-negotiable (CLAUDE.md).**

**Reusable testing pattern (Weeks 4–6, keep using):** sequential-phase actions can't all be "open now" at one instant, so tests advance a CPI by re-calling `PUT /timeline` (replace-all, not phase-gated) to re-anchor which phase brackets now — see `timelineOpening(phase)` in the ideas/selection/week6 tests. Per-stage evaluators assigned via `stage_evaluators`; CPI-level Head Judge (one per CPI, from Week 3) kept rather than the spec's alt "per-stage Head Judge" — revisit if Week 7 needs per-stage HJ.

### Prior weeks (condensed)
Week 1: TS/Express/Prisma scaffold, JWT (15m access + 7d rotating refresh in Redis)+bcrypt(12) auth, forced first-login password change, RBAC middleware (`requireAuth`/`requireRole`), CI against real Postgres+Redis. Week 2: student bulk provisioning (CSV→CSPRNG temp pw→BullMQ credential emails, `student_provisioning_log`), lecturer self-registration + admin approval (enumeration-safe login gate), audit log middleware on all writes (SHA-256 payload hash, never stored). Week 3: CPI creation + 10-phase timeline engine, `requirePhase()` phase-gating middleware, supervisor addition with automatic mode determination (first supervisor invite→SUPERVISOR_LED; `finalize-coordinator-managed`→COORDINATOR_MANAGED), evaluator+Head Judge assignment, `POST /users/:id/assign-coordinator`, shared `assertRole()`. Week 4: group formation (leader creates+invites by email, accept/decline, one-accepted-group-per-CPI enforced in service, groups "lock" via STUDENT_REGISTRATION phase-gate — `locked` derived from timeline), idea posting (single endpoint branching by actor capacity), coordinator approve/reject student ideas. **Idea visibility enforced in the list query, not stored per-idea** — student sees public ideas + own group's only, never other groups' (both modes, even after approval). `groups`/`ideas`/`selection` routers mount at `/courses/:cpiId/...` with mergeParams. Week 5: EOI + Mutual Confirmation (`selection` module). Supervisor-Led: group ranks ≤3 supervisor ideas / flags own idea SEEKING_SUPERVISOR / supervisors mark WILLING → group `select` → `ProjectSelection` PENDING → chosen supervisor accepts (conflict resolution = group picks which willing supervisor via `supervisorUserId`, only they may respond). Coordinator-Managed (minimal): select coordinator/own-approved idea → coordinator confirms. Tables `interest_expressions`+`project_selections`. Shared `getStudentGroupId`/`getAcceptedSupervisorLecturerId` in `cpiMembership.ts`. **Key design decision (still holds):** CPI-scoped roles live in `cpi_supervisors`/`cpi_evaluators` junctions, NOT on `User.role`. Coordinator CPI-scope enforced via `loadOwnedCpi` (404 non-existent, 403 not-owned). Deviations: BullMQ not Bull, Prisma v6 (v7 dropped `datasource url` syntax), ioredis pinned 5.10.1 to match BullMQ, added `LECTURER` base role. SMTP env-driven w/ dev jsonTransport fallback — real creds needed before pilot. Seed: admin@ / Admin#Pilot2026, coordinator@ / Coord#Pilot2026, lecturer1&2@ / Lect#Pilot2026, student1@ (password may have drifted from seed — upsert doesn't reset existing users).
